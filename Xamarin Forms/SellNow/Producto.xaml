<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="SellNow.Producto">
	<StackLayout>
		<SearchBar Placeholder="" Text="" HeightRequest="50"></SearchBar>
		<Image HeightRequest="180" Source="http://i.imgur.com/psJSEMU.png" />
		<Label	Font="Bold,20" Margin="15,0,15,0" Text="Producto 1"/>
		<Label  Text="Descripción del producto, detallando aspectos importantes del producto para que se puje por él" 
				Margin="15,0,15,0"/>
		
		<Grid>
		    <Grid.RowDefinitions>
		      <RowDefinition Height="Auto" />
		      <RowDefinition Height="*" />
		    </Grid.RowDefinitions>		   			
		    <Grid.ColumnDefinitions>
		      <ColumnDefinition Width="100" />
		    </Grid.ColumnDefinitions>

		    <Label
					Margin="15" 
					TextColor="Blue"
					Text="Vendedor"
		            Grid.Row="1" 
					Grid.Column="2"/>
			<Label  Text="40$"
					Margin="45,10,0,0"
			 		Grid.Row="2" 
					Grid.Column="0"/>
			<Button Text="Pujar"
					Margin="0,10,25,0"
			 		Grid.Row="2" 
					Grid.Column="2"/>
					
		</Grid>
		<Slider
			Margin="15"
		    x:Name="DesSlider"
	        Minimum="0"
	        Maximum="5"
	        Value="0"
	        VerticalOptions="CenterAndExpand"/>

	</StackLayout>
</ContentPage>